[
  {
    "subheading": "Basic Types & Type Inference",
    "explanation": "TypeScript adds a static type system on top of JavaScript. It catches errors at compile time — before your code runs — by analyzing types across your entire codebase. The compiler (`tsc`) strips all type annotations, producing plain JavaScript that runs anywhere. Think of types as documentation that the compiler enforces.\n\nTypeScript infers types automatically from context, so you don't always need explicit annotations. `let x = 5` infers `number`; `const x = 5` infers `5` (literal type). The compiler uses control flow analysis to narrow types within `if` blocks, `typeof` checks, and error guards.",
    "example": "```typescript\n// Explicit types\nlet name: string = \"Alice\";\nlet age: number = 30;\nlet isActive: boolean = true;\nlet items: string[] = [\"a\", \"b\", \"c\"];\nlet pair: [string, number] = [\"Alice\", 30]; // tuple\n\n// Type inference — compiler figures it out\nlet count = 42;           // inferred as number\nconst PI = 3.14159;       // inferred as 3.14159 (literal)\nlet mixed = [1, \"two\", true]; // (string | number | boolean)[]\n\n// Type narrowing\nfunction process(value: string | number) {\n  if (typeof value === \"string\") {\n    // TypeScript knows: value is string here\n    console.log(value.toUpperCase());\n  } else {\n    // TypeScript knows: value is number here\n    console.log(value.toFixed(2));\n  }\n}\n\n// Union types\ntype Status = \"pending\" | \"active\" | \"completed\";\nlet status: Status = \"active\";\n// status = \"invalid\"; // Error!\n\n// Literal types\nfunction setAlignment(align: \"left\" | \"center\" | \"right\") {\n  // ...\n}\nsetAlignment(\"left\");   // OK\n// setAlignment(\"justify\"); // Error!\n\n// Type assertions (use sparingly)\nconst input = document.getElementById(\"name\") as HTMLInputElement;\nconsole.log(input.value); // TypeScript now knows it's HTMLInputElement\n\n// satisfies operator (TypeScript 5.0)\nconst config = {\n  port: 3000,\n  host: \"localhost\",\n} satisfies Record<string, string | number>;\nconfig.port; // type is number (not string | number)\n```",
    "key_takeaway": "TypeScript infers types from context — explicit annotations are needed only when inference can't determine the type (function parameters, empty arrays, complex unions).",
    "try_it": "Create a `formatInput` function that accepts `string | number | boolean` and returns a formatted string. Use type narrowing with `typeof` to handle each case differently."
  },
  {
    "subheading": "Interfaces & Type Aliases",
    "explanation": "Interfaces and type aliases both define the shape of objects, but they differ in key ways. Interfaces are extendable — you can declare the same interface multiple times and TypeScript merges them. Type aliases are more flexible — they can represent unions, intersections, tuples, and primitive aliases. Use interfaces for object shapes that may be extended; use type aliases for everything else.\n\nBoth support optional properties (`?`), readonly properties, and function signatures. The `extends` keyword lets interfaces inherit from other interfaces; type aliases use intersection (`&`) for the same effect.",
    "example": "```typescript\n// Interface — extendable, mergeable\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n  avatar?: string;  // optional\n}\n\ninterface Admin extends User {\n  role: \"admin\";\n  permissions: string[];\n}\n\n// Interface merging (declaration merging)\ninterface User {\n  bio?: string;  // merged into User above\n}\n\n// Type alias — flexible, can represent unions\ntype ID = string | number;\ntype Coordinates = [number, number];\ntype Status = \"pending\" | \"active\" | \"completed\";\n\ntype ApiResponse<T> = {\n  data: T;\n  status: number;\n  message: string;\n};\n\n// Intersection type (like extends)\ntype Timestamped = {\n  createdAt: Date;\n  updatedAt: Date;\n};\n\ntype UserWithTimestamps = User & Timestamped;\n\n// Function signatures\ntype EventHandler = (event: MouseEvent) => void;\ntype AsyncFetcher<T> = (url: string) => Promise<T>;\n\n// Readonly and mapped types\ntype ReadonlyUser = Readonly<User>;\ntype PartialUser = Partial<User>;\ntype RequiredUser = Required<User>;\n\n// Custom mapped type\ntype Nullable<T> = { [K in keyof T]: T[K] | null };\ntype NullableUser = Nullable<User>;\n\n// Usage\nconst user: User = {\n  id: 1,\n  name: \"Alice\",\n  email: \"alice@example.com\",\n};\n\nconst admin: Admin = {\n  id: 2,\n  name: \"Bob\",\n  email: \"bob@example.com\",\n  role: \"admin\",\n  permissions: [\"read\", \"write\", \"delete\"],\n};\n\nconst response: ApiResponse<User[]> = {\n  data: [user, admin],\n  status: 200,\n  message: \"OK\",\n};\n```",
    "key_takeaway": "Interfaces for extendable object shapes with declaration merging; type aliases for unions, intersections, and complex types. Both support optional (`?`) and readonly modifiers.",
    "try_it": "Define a `PaginatedResponse<T>` type with `data: T[]`, `page: number`, `totalPages: number`, and `hasNext: boolean`. Use it with `User` and `Product` interfaces."
  },
  {
    "subheading": "Generics",
    "explanation": "Generics let you write functions, classes, and types that work with any type while preserving type safety. They're like type parameters — placeholders filled in when the function is called. Instead of writing separate functions for `number[]` and `string[]`, you write one `Array<T>` that works for all types.\n\nGenerics are essential for reusable libraries. Without them, you'd either lose type information (using `any`) or duplicate code for each type. Generic constraints (`extends`) limit what types are accepted, ensuring the function can actually operate on the input.",
    "example": "```typescript\n// Generic function\nfunction identity<T>(value: T): T {\n  return value;\n}\n\nconst num = identity(42);        // type: number\nconst str = identity(\"hello\");   // type: string\n\n// Generic with constraints\ninterface HasLength {\n  length: number;\n}\n\nfunction logLength<T extends HasLength>(value: T): T {\n  console.log(`Length: ${value.length}`);\n  return value;\n}\n\nlogLength(\"hello\");      // OK: string has length\nlogLength([1, 2, 3]);    // OK: array has length\n// logLength(42);         // Error: number has no length\n\n// Generic class\nclass Stack<T> {\n  private items: T[] = [];\n\n  push(item: T): void {\n    this.items.push(item);\n  }\n\n  pop(): T | undefined {\n    return this.items.pop();\n  }\n\n  peek(): T | undefined {\n    return this.items[this.items.length - 1];\n  }\n\n  get size(): number {\n    return this.items.length;\n  }\n}\n\nconst numStack = new Stack<number>();\nnumStack.push(1);\nnumStack.push(2);\nconsole.log(numStack.pop()); // 2\n\n// Multiple generic parameters\nfunction merge<T, U>(obj1: T, obj2: U): T & U {\n  return { ...obj1, ...obj2 };\n}\n\nconst merged = merge({ name: \"Alice\" }, { age: 30 });\n// type: { name: string } & { age: number }\n\n// Generic with default type\nclass ApiResponse<T = unknown> {\n  constructor(\n    public data: T,\n    public status: number = 200\n  ) {}\n}\n\nconst res = new ApiResponse({ users: [] }, 200);\nconst res2 = new ApiResponse<string>(\"OK\");\n\n// Conditional types\ntype IsString<T> = T extends string ? \"yes\" : \"no\";\ntype A = IsString<string>;  // \"yes\"\ntype B = IsString<number>;  // \"no\"\n\n// Infer keyword\ntype ReturnType<T> = T extends (...args: any[]) => infer R ? R : never;\ntype FnReturn = ReturnType<() => string>; // string\n```",
    "key_takeaway": "Generics preserve type information across function calls — use `<T>` for type parameters, `extends` for constraints, and conditional types for type-level logic.",
    "try_it": "Write a generic `groupBy<T, K extends keyof T>(items: T[], key: K): Record<string, T[]>` that groups an array of objects by a specified property."
  },
  {
    "subheading": "Union & Intersection Types",
    "explanation": "Union types (`A | B`) represent values that can be one of several types — a variable that's either a string or a number. Intersection types (`A & B`) combine multiple types into one — an object that must satisfy all types simultaneously. These are TypeScript's type algebra operators.\n\nDiscriminated unions use a common literal property (the discriminant) to distinguish between union members in control flow. This is the TypeScript pattern for tagged data — safer than checking `typeof` and more expressive than class hierarchies.",
    "example": "```typescript\n// Union types\ntype StringOrNumber = string | number;\n\nfunction format(value: StringOrNumber): string {\n  if (typeof value === \"string\") {\n    return value.toUpperCase();\n  }\n  return value.toFixed(2);\n}\n\n// Discriminated union — the TypeScript pattern\ntype Circle = { kind: \"circle\"; radius: number };\ntype Rectangle = { kind: \"rectangle\"; width: number; height: number };\ntype Triangle = { kind: \"triangle\"; base: number; height: number };\n\ntype Shape = Circle | Rectangle | Triangle;\n\nfunction area(shape: Shape): number {\n  switch (shape.kind) {\n    case \"circle\":\n      return Math.PI * shape.radius ** 2;\n    case \"rectangle\":\n      return shape.width * shape.height;\n    case \"triangle\":\n      return 0.5 * shape.base * shape.height;\n  }\n}\n\nconsole.log(area({ kind: \"circle\", radius: 5 })); // 78.54\n\n// Exhaustive checking\nfunction assertNever(x: never): never {\n  throw new Error(`Unexpected: ${x}`);\n}\n\nfunction describe(shape: Shape): string {\n  switch (shape.kind) {\n    case \"circle\": return `Circle r=${shape.radius}`;\n    case \"rectangle\": return `Rect ${shape.width}x${shape.height}`;\n    case \"triangle\": return `Triangle b=${shape.base}`;\n    default: return assertNever(shape); // error if case missing!\n  }\n}\n\n// Intersection types\ntype Draggable = { drag: () => void };\ntype Resizable = { resize: (w: number, h: number) => void };\n\ntype UIWidget = Draggable & Resizable;\n\nconst widget: UIWidget = {\n  drag() { console.log(\"dragging\"); },\n  resize(w, h) { console.log(`resizing to ${w}x${h}`); },\n};\n\n// Discriminated union for API responses\ntype Success<T> = { status: \"ok\"; data: T };\ntype Failure = { status: \"error\"; message: string };\ntype Result<T> = Success<T> | Failure;\n\nfunction handleResult<T>(result: Result<T>): T | null {\n  if (result.status === \"ok\") {\n    return result.data;  // narrowed to Success<T>\n  }\n  console.error(result.message);  // narrowed to Failure\n  return null;\n}\n```",
    "key_takeaway": "Discriminated unions with a `kind` field and `switch` statement give you exhaustive, type-safe pattern matching — the most important TypeScript pattern for complex data.",
    "try_it": "Create a discriminated union for a traffic light (`red`, `yellow`, `green`) with associated data (seconds remaining). Write a `nextLight` function that returns the next state with correct timing."
  },
  {
    "subheading": "Utility Types",
    "explanation": "Utility types are built-in type transformations that create new types from existing ones. `Partial<T>` makes all properties optional. `Required<T>` makes them required. `Pick<T, K>` selects specific properties. `Omit<T, K>` removes properties. `Record<K, V>` creates an object type with keys K and values V.\n\nThese types eliminate the need to define separate interfaces for every variation of a data shape. Instead of creating `UserCreateInput`, `UserUpdateInput`, and `UserResponse` manually, you derive them from a single `User` type using utility types.",
    "example": "```typescript\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n  password: string;\n  avatar?: string;\n  createdAt: Date;\n}\n\n// Partial — all properties optional (for updates)\ntype UserUpdate = Partial<Pick<User, \"name\" | \"email\" | \"avatar\">>;\nconst update: UserUpdate = { name: \"New Name\" }; // OK\n\n// Omit — remove properties (for responses)\ntype UserResponse = Omit<User, \"password\">;\nconst response: UserResponse = {\n  id: 1, name: \"Alice\", email: \"a@b.com\", createdAt: new Date()\n};\n\n// Pick — select specific properties (for creation)\ntype UserCreate = Pick<User, \"name\" | \"email\" | \"password\">;\nconst newUser: UserCreate = { name: \"Bob\", email: \"b@b.com\", password: \"secret\" };\n\n// Record — object type with specific keys and values\ntype Role = \"admin\" | \"user\" | \"guest\";\ntype RolePermissions = Record<Role, string[]>;\nconst permissions: RolePermissions = {\n  admin: [\"read\", \"write\", \"delete\"],\n  user: [\"read\", \"write\"],\n  guest: [\"read\"],\n};\n\n// Readonly\ntype FrozenUser = Readonly<User>;\n// frozenUser.name = \"new\"; // Error!\n\n// ReturnType & Parameters\ntype Fn = (a: string, b: number) => boolean;\ntype FnReturn = ReturnType<Fn>;     // boolean\n  type FnParams = Parameters<Fn>;     // [a: string, b: number]\n\n// Awaited — unwrap Promise type\ntype AsyncResult = Promise<Promise<string>>;\ntype Result = Awaited<AsyncResult>; // string\n\n// NonNullable\ntype MaybeString = string | null | undefined;\ntype DefiniteString = NonNullable<MaybeString>; // string\n\n// Extract & Exclude\ntype AllTypes = string | number | boolean | null;\ntype OnlyStrings = Extract<AllTypes, string>;        // string\n  type NotNull = Exclude<AllTypes, null>;  // string | number | boolean\n\n// Template literal types\ntype EventName = `on${Capitalize<\"click\" | \"hover\" | \"focus\">}`;\n// \"onClick\" | \"onHover\" | \"onFocus\"\n\ntype CSSProperty = \"margin\" | \"padding\";\ntype CSSDirection = \"top\" | \"right\" | \"bottom\" | \"left\";\ntype CSSKey = `${CSSProperty}-${CSSDirection}`;\n// \"margin-top\" | \"margin-right\" | ... | \"padding-left\"\n```",
    "key_takeaway": "Derive input/output types from a single source of truth using `Partial`, `Pick`, `Omit`, and `Record` — never manually duplicate interfaces for API variations.",
    "try_it": "From a `Product` interface, derive `ProductCreate` (no id, no timestamps), `ProductUpdate` (all optional, no id), and `ProductSummary` (only name, price, category)."
  },
  {
    "subheading": "Decorators & Advanced Patterns",
    "explanation": "Decorators are functions that modify classes, methods, properties, or parameters at definition time. They're widely used in frameworks like Angular and NestJS for dependency injection, logging, and validation. The `@` syntax applies a decorator to the target below it.\n\nAdvanced TypeScript patterns include conditional types, template literal types, mapped types, and the `infer` keyword. These enable type-level programming — computing types from other types. While powerful, they should be used sparingly in application code; they shine in library type definitions.",
    "example": "```typescript\n// Method decorator — logging\ndefunction Log(target: any, propertyKey: string, descriptor: PropertyDescriptor) {\n  const original = descriptor.value;\n  descriptor.value = function (...args: any[]) {\n    console.log(`Calling ${propertyKey} with`, args);\n    const result = original.apply(this, args);\n    console.log(`${propertyKey} returned`, result);\n    return result;\n  };\n}\n\n// Class decorator\ndefunction Sealed(constructor: Function) {\n  Object.seal(constructor);\n  Object.seal(constructor.prototype);\n}\n\n// Parameterized decorator factory\ndefunction RateLimit(limit: number, windowMs: number) {\n  const calls: number[] = [];\n  return function (target: any, propertyKey: string, descriptor: PropertyDescriptor) {\n    const original = descriptor.value;\n    descriptor.value = function (...args: any[]) {\n      const now = Date.now();\n      const recentCalls = calls.filter(t => now - t < windowMs);\n      if (recentCalls.length >= limit) {\n        throw new Error(`Rate limit exceeded for ${propertyKey}`);\n      }\n      calls.push(now);\n      return original.apply(this, args);\n    };\n  };\n}\n\n@Sealed\nclass Calculator {\n  @Log\n  add(a: number, b: number): number {\n    return a + b;\n  }\n\n  @RateLimit(5, 1000)\n  expensiveOperation(): void {\n    console.log(\"Working...\");\n  }\n}\n\n// Conditional types with infer\ntype UnpackPromise<T> = T extends Promise<infer U> ? U : T;\ntype A = UnpackPromise<Promise<string>>;  // string\n  type B = UnpackPromise<number>;           // number\n\n// Distributive conditional types\ntype ToArray<T> = T extends any ? T[] : never;\ntype Result = ToArray<string | number>; // string[] | number[]\n\n// Mapped types with as\ntype Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]\n};\n\ntype UserGetters = Getters<{ name: string; age: number }>;\n// { getName: () => string; getAge: () => number }\n\n// Recursive types\ntype DeepReadonly<T> = {\n  readonly [K in keyof T]: T[K] extends object ? DeepReadonly<T[K]> : T[K];\n};\n```",
    "key_takeaway": "Decorators modify class members at definition time — use them for cross-cutting concerns (logging, validation, rate limiting). Conditional types with `infer` enable type-level computation.",
    "try_it": "Write a `@Validate` decorator that checks if method arguments match expected types. Use `Reflect.getMetadata` to store validation rules on parameters."
  },
  {
    "subheading": "TypeScript Configuration & Compilation",
    "explanation": "The `tsconfig.json` file controls TypeScript's compiler behavior. Strict mode (`\"strict\": true`) enables all strict checks — always use it for new projects. The `target` setting determines the JavaScript version output (ES2020+ for modern Node/browsers, ES5 for legacy). Module system (`\"module\": \"ESNext\"` or `\"CommonJS\"`) controls import/export syntax.\n\nPath mapping (`paths`) creates import aliases. `include`/`exclude` control which files are compiled. Declaration files (`.d.ts`) provide types for JavaScript libraries. Understanding the compiler options prevents the \"it works in my IDE but not in production\" problem.",
    "example": "```json\n// tsconfig.json\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true,\n    \"resolveJsonModule\": true,\n    \"isolatedModules\": true,\n    \"noUnusedLocals\": true,\n    \"noUnusedParameters\": true,\n    \"noImplicitReturns\": true,\n    \"noFallthroughCasesInSwitch\": true,\n    \"declaration\": true,\n    \"declarationMap\": true,\n    \"sourceMap\": true,\n    \"outDir\": \"./dist\",\n    \"rootDir\": \"./src\",\n    \"baseUrl\": \".\",\n    \"paths\": {\n      \"@/*\": [\"./src/*\"],\n      \"@components/*\": [\"./src/components/*\"],\n      \"@utils/*\": [\"./src/utils/*\"]\n    },\n    \"types\": [\"node\", \"jest\"]\n  },\n  \"include\": [\"src/**/*\"],\n  \"exclude\": [\"node_modules\", \"dist\", \"**/*.test.ts\"]\n}\n```\n\n```typescript\n// Path aliases in action\nimport { User } from \"@/types/user\";\nimport { Button } from \"@components/Button\";\nimport { formatDate } from \"@utils/date\";\n\n// Declaration file for JavaScript library\n// types/my-lib.d.ts\ndeclare module \"my-lib\" {\n  export function process(data: string): { result: string; success: boolean };\n  export const VERSION: string;\n}\n\n// Ambient declarations for global variables\ndeclare const __APP_VERSION__: string;\ndeclare const API_URL: string;\n\n// Type-only imports (removed at compile time)\nimport type { User } from \"./types\";\nimport { createUser } from \"./api\"; // value import\n\n// Checking types without emitting JS\n// npx tsc --noEmit\n\n// Project references for monorepos\n// tsconfig.json (root)\n// {\n//   \"references\": [\n//     { \"path\": \"./packages/core\" },\n//     { \"path\": \"./packages/ui\" }\n//   ]\n// }\n```",
    "key_takeaway": "Always use `\"strict\": true`. Use path aliases (`@/*`) for clean imports. Run `tsc --noEmit` in CI to catch type errors without generating files.",
    "try_it": "Set up a TypeScript project with strict mode, path aliases for `@/` and `@components/`, and a custom declaration file for a fictional npm package. Verify imports work with both `tsc` and your IDE."
  },
  {
    "subheading": "Practice Problems",
    "explanation": "TypeScript interview problems test your ability to leverage the type system for safer code. Key patterns include generics for reusable functions and classes, discriminated unions for state machines, mapped types for transformations, and conditional types for complex type-level logic. Understanding utility types (Partial, Required, Pick, Omit, Record) is essential.\n\nBeyond type-level challenges, TypeScript problems often mirror JavaScript problems but with added emphasis on type safety: implementing typed versions of common utilities, designing API interfaces, and handling null/undefined with strict mode. The type system is your biggest ally — use it to catch bugs at compile time rather than runtime.",
    "example": "```typescript\n// Group Anagrams (LC #49) — TypeScript with generics\nfunction groupAnagrams(strs: string[]): string[][] {\n    const map = new Map<string, string[]>();\n    for (const str of strs) {\n        const key: string = str.split('').sort().join('');\n        const group = map.get(key) ?? [];\n        group.push(str);\n        map.set(key, group);\n    }\n    return Array.from(map.values());\n}\n\n// Type-safe version with generics\nfunction groupBy<T, K extends string | number>(\n    items: T[],\n    keyFn: (item: T) => K\n): Map<K, T[]> {\n    const map = new Map<K, T[]>();\n    for (const item of items) {\n        const key = keyFn(item);\n        const group = map.get(key) ?? [];\n        group.push(item);\n        map.set(key, group);\n    }\n    return map;\n}\n\n// Test\nconst words = [\"eat\",\"tea\",\"tan\",\"ate\",\"nat\",\"bat\"];\nconsole.log(groupAnagrams(words));\nconsole.log(groupBy(words, w => w.split('').sort().join('')));\n```",
    "key_takeaway": "Use generics for reusable functions, discriminated unions for type-safe state, and the strict compiler to catch bugs before runtime.",
    "try_it": "Try solving the first Easy problem with full strict TypeScript — no `any` types allowed.",
    "complexity": "Varies by problem",
    "practice_challenges": [
      {
        "title": "Two Sum",
        "number": 1,
        "difficulty": "Easy",
        "url": "https://leetcode.com/problems/two-sum/",
        "pattern": "Hash Map",
        "hint": "Use a Map<number, number> to store value-to-index mappings. For each num, check if (target - num) exists.",
        "description": "Given an array of integers and a target, return indices of two numbers that add up to the target.",
        "example_input": "nums = [2, 7, 11, 15], target = 9",
        "example_output": "[0, 1]"
      },
      {
        "title": "Valid Anagram",
        "number": 242,
        "difficulty": "Easy",
        "url": "https://leetcode.com/problems/valid-anagram/",
        "pattern": "Hash Map / Counter",
        "hint": "Use a Map<string, number> to count character frequencies, then compare both maps.",
        "description": "Given two strings, determine if one is an anagram of the other.",
        "example_input": "s = \"anagram\", t = \"nagaram\"",
        "example_output": "true"
      },
      {
        "title": "LRU Cache",
        "number": 146,
        "difficulty": "Medium",
        "url": "https://leetcode.com/problems/lru-cache/",
        "pattern": "Hash Map + Doubly Linked List",
        "hint": "Use a Map for O(1) lookup and a doubly linked list for O(1) eviction order. Move accessed nodes to the front.",
        "description": "Design a Least Recently Used cache that supports get and put in O(1) time.",
        "example_input": "LRUCache(2); put(1,1); put(2,2); get(1); put(3,3); get(2)",
        "example_output": "1, -1"
      },
      {
        "title": "Implement Trie (Prefix Tree)",
        "number": 208,
        "difficulty": "Medium",
        "url": "https://leetcode.com/problems/implement-trie-prefix-tree/",
        "pattern": "Trie / Tree Design",
        "hint": "Each node has a Map<char, Node> for children and a boolean isEnd. Insert traverses character by character.",
        "description": "Implement a trie with insert, search, and startsWith methods.",
        "example_input": "insert(\"apple\"); search(\"app\"); startsWith(\"app\")",
        "example_output": "false, true"
      },
      {
        "title": "Median of Two Sorted Arrays",
        "number": 4,
        "difficulty": "Hard",
        "url": "https://leetcode.com/problems/median-of-two-sorted-arrays/",
        "pattern": "Binary Search",
        "hint": "Binary search on the partition point of the shorter array. Find a split where all left elements <= all right elements.",
        "description": "Given two sorted arrays, find the median of the combined sorted array in O(log(m+n)) time.",
        "example_input": "nums1 = [1, 3], nums2 = [2]",
        "example_output": "2.0"
      }
    ]
  }
]